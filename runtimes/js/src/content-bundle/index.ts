/**
 * @packageDocumentation
 *
 * Module exposing a handle on the compiled content bundle for the host application at hand,
 * which the latter can set (e.g., during initialization) using a helper function exposed here.
 */

export * from "./constants";
export * from "./registration";
